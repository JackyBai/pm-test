General Info
============
- In order to run the tests, invoke as root:

   ./test.sh

- If you want to run a subset of the tests, do:
        cd to subdir 
	run the individual shell script
Other info
==========
--each txt file in susdir contains the relevant descriptive 
  information about the test case.
--

